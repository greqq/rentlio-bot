# Webhook server requirements
# For running on Raspberry Pi

# Web server
flask>=3.0.0

# Production server (recommended for Raspberry Pi)
gunicorn>=21.0.0

# HTTP client (for Telegram notifications)
requests>=2.31.0

# Note: SQLite is included in Python standard library (no extra install needed!)
